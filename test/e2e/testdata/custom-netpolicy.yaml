apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: test-custom-np
  namespace: {{NAMESPACE}}
spec:
  podSelector:
    matchLabels:
      test: "true"
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: test-app
