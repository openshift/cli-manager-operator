apiVersion: v1
kind: Service
metadata:
  name: cli-manager-operator-metrics
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: cli-manager-operator-serving-cert
  labels:
    name: openshift-cli-manager-operator
spec:
  selector:
    name: openshift-cli-manager-operator
  ports:
    - name: https
      port: 8443
      targetPort: 8443
      protocol: TCP
